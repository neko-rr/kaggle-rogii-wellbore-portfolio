# Pretrain Gate — CHK-688 L relearn retrain (hard20)

- **Date:** 2026-08-04
- **CHK:** CHK-688
- **NB:** `my-notebook/tip-cv-chk688-learned-retrain-h20/`
- **Status:** **PASS** (local Tier0 + lint/private + ban-gate escalation bypass)
- **GPU push:** **v1 RUNNING**（2026-08-04 · ユーザー GPU OK）  
  https://www.kaggle.com/code/kazeneko77/tip-cv-chk688-learned-retrain-h20  
  · run-log: `my-ran-notebook/tip-cv-chk688-learned-retrain-h20/run-log.md`

## Tier 0（Lint / 静的）

- [x] notebook 生成（533 派生 · FORCE retrain · stop after learned）
- [x] notebook JSON load OK
- [x] AST parse all code cells（50 cells）— 533 由来 nested f-string 1 箇所を修正済み
- [x] `FORCE_RETRAIN_LEARNED` / retrain fallthrough / `TIP_CV_STOP_AFTER_LEARNED`
- [x] `assert-kaggle-private` → PASS（`is_private=true` · GPU レーン）
- [x] `enable_internet=false` · `model_sources` 空
- [x] hard20 well ファイル（horizontal + typewell）ディスク上
- [x] `sample_submission` 列 `id,tvt`
- [ ] WARN: ローカル `.venv` に lightgbm 無し → micro train は SKIP（Kaggle 本線）

## Tier 1

- micro lightgbm（ローカル）: **SKIP_NO_LGB**
- Full hard20 `train_stack`: **Kaggle GPU 完走後**に実測（今回は静的 PASS のみ）

## Hypothesis ban-gate（pre）

| 項目 | 結果 |
|---|---|
| action_type | **T3**（L 再学習＝自前 rewrite） |
| streak 生 | T3=5（745/712 系）→ 通常 FAIL |
| 対応 | `improvement-loop-allowlist.json` **CHK-688** · bypass=`escalation`（F043 が許可する別仮説） |
| failures キーワード | **照合継続**（F043 文言は当たる設計） |

再実行コマンド（GPU 前に必ず）:

```powershell
.\scripts\run-hypothesis-ban-gate.ps1 -ChkId CHK-688 -ActionType T3 `
  -Hypothesis "retrain learned LGB/CB tip-cv hard20 dump new L face" `
  -Mechanism "FORCE_RETRAIN train_stack; stop after learned CSV; no submit" `
  -Phase pre -ExpDir ".\20260722-rogii-wellbore\exp"
```

## Notes

- F015: new L は中間材料。FINAL 昇格は residual dual vs 666 後
- F043: 現 L 上 α 言い換えは禁止。本経路は **L 面の再学習**
- Submit: この NB では禁止。提出は別途ユーザー OK

Report: `exp/work/colab-final-t2/out-688-pretrain-gate/pretrain-gate-report.json`

