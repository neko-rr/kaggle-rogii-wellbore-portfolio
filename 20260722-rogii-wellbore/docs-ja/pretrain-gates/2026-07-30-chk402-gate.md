# Pretrain Gate — CHK-402

> profile: tabular  
> tier: 0+1+2  
> result: PASS  
> chk: CHK-402  
> execution: local-static + inherited-kaggle-smoke

## Tier 0

- [x] CHK-401 と同じ non-hard20×20 を固定
- [x] train well の `TVT_input` NaN 行だけを予測・採点
- [x] well 単位分離を維持し、Random CV を使わない
- [x] 出力は診断用で提出禁止
- [x] `kernel-metadata.json`: Private / GPU / Internet OFF
- [x] 外部依存は既存 tip と同一で、ライセンス台帳に登録済み

## Tier 1

- [x] 同一コード本体の CHK-256 Kaggle GPU 実行で selector と内部面出力を確認済み
- [x] 今回の変更は allowlist・識別子・診断表示のみ
- [x] allowlist は20件・重複なしをセル冒頭で assert

## Tier 2

- [x] CHK-398 で同一20井の ranker / soft / CF が有限値・全件coverage
- [x] CHK-402 は不足していた tip FINAL を同じ20井で生成する matched baseline
- [x] 採否は pooled RMSE、井別勝率、error Pearson、blend screen で判定

## 判定

**PASS**。Kaggle GPU で CHK-402 を1回実行してよい。完走後も自動提出しない。
