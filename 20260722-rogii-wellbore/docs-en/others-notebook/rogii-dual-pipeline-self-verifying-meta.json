{
  "slug": "rogii-dual-pipeline-self-verifying",
  "title": "ROGII Dual Pipeline + Self-Verifying",
  "gpu": false,
  "internet": false,
  "datasets": [
    "phongnguyn23021656/koolbox-offline",
    "fleongg/rogii-claude-models-pub",
    "ravaghi/wellbore-geology-prediction-artifacts"
  ],
  "kernels": [],
  "n_cells": 63,
  "imports": [
    "catboost",
    "glob",
    "joblib",
    "koolbox",
    "lightgbm",
    "matplotlib",
    "multiprocessing",
    "numba",
    "numpy",
    "os",
    "os,",
    "pandas",
    "pathlib",
    "scipy",
    "seaborn",
    "sklearn",
    "sys,",
    "time",
    "warnings"
  ],
  "defs": [
    "CFG",
    "tvt_from_contacts",
    "load_well",
    "run_particle_filter",
    "run_pf_lik_ensemble",
    "run_pf_lik_ensemble_scales",
    "beam_search",
    "run_beam_ensemble",
    "selector_well_code",
    "parse_selector_variant",
    "apply_selector_variant",
    "_interp1",
    "_resamp",
    "_beam_jit",
    "_pf_ancc",
    "_pf_z",
    "_grid",
    "_gr_sig",
    "_nn",
    "_smooth",
    "beam_search",
    "run_pf_ancc",
    "run_pf_z",
    "robust_slope",
    "affine_cal",
    "seg_b_well",
    "multi_scale_ncc",
    "FormationPlaneKNN",
    "DenseANCCImputer",
    "build_well",
    "build_dataset",
    "apply_pp",
    "sg_smooth",
    "_robfit",
    "_find_data",
    "CFG",
    "_demo_well",
    "load_well",
    "rmse",
    "fig_overview"
  ],
  "md_heads": [
    [
      0,
      "# 🛢️ ROGII Wellbore Geology Prediction — Dual-Pipeline Blend + a Self-Verifying Physical Override"
    ],
    [
      0,
      "## Architecture"
    ],
    [
      0,
      "## Why each piece is there"
    ],
    [
      0,
      "## What's new vs. the public blends — the *guard*"
    ],
    [
      0,
      "## How to run"
    ],
    [
      0,
      "## Credits"
    ],
    [
      1,
      "# Part 1 · Pipeline A — \"ridge-sp45\""
    ],
    [
      1,
      "### 1.1 · Offline setup — install `koolbox` from the mounted wheel (no internet needed)"
    ],
    [
      3,
      "### 1.2 · Imports & configuration"
    ],
    [
      6,
      "### 1.3 · Physics toolbox"
    ],
    [
      8,
      "### 1.4 · Numba kernels + per-well feature builder"
    ],
    [
      10,
      "### 1.5 · Build (or fast-load) the feature table"
    ],
    [
      12,
      "### 1.6 · Training setup — booster configs & CV splitter"
    ],
    [
      15,
      "### 1.7 · LightGBM (pre-trained fast-path when the artifacts are mounted)"
    ],
    [
      17,
      "### 1.8 · CatBoost"
    ],
    [
      19,
      "### 1.9 · Ridge meta-ensemble over the boosters' out-of-fold predictions"
    ],
    [
      22,
      "### 1.10 · Post-processing — warm-up damping (τ) + Savitzky–Golay smoothing"
    ],
    [
      25,
      "### 1.11 · Inference — Ridge (tabular) predictions"
    ],
    [
      28,
      "### 1.12 · Inference — heuristic physical model (per-well PF selector; exact contacts where available)"
    ],
    [
      31,
      "### 1.13 · Blend + robust projection"
    ],
    [
      34,
      "### 1.14 · Pipeline A results — and preserve its output for the final blend"
    ],
    [
      37,
      "# Part 2 · Pipeline B — \"fleongg\" (likelihood-PF + GBM stack)"
    ],
    [
      37,
      "### 2.1 · Environment & data discovery"
    ],
    [
      39,
      "### 2.2 · EDA — the problem, visually *(plots are off by default; set `SHOW_FIGS=1` to see them)*"
    ],
    [
      42,
      "### 2.3 · Trackers — likelihood-weighted PF (128 seeds × 4 scales) + beam search"
    ]
  ],
  "first_md": "# 🛢️ ROGII Wellbore Geology Prediction — Dual-Pipeline Blend + a Self-Verifying Physical Override\n\n**TL;DR** — This notebook blends **two independently-built geosteering pipelines** (decorrelated errors → free accuracy), then finishes with a **guarded physical override**: where a test well can be reconstructed *exactly* from formation contacts, we use the exact physics — but **only after the reconstruction proves itself at runtime** on that well's known prefix. The guard makes the final step **never worse** than the plain blend, by construction.\n\n## Architecture\n\n```\n                ┌───────────────────────────────────────┐\n                │ PIPELINE A · \"ridge-sp45\"             │\n train + test ─►│  particle filter (128 seeds×4 scales)│─► sub_A ─┐\n                │  + beam search + per-well selector    │          │ 0.55\n                │  + LightGBM/CatBoost + Ridge meta     │          ▼\n                │  + warm-up damping + SG smoothing     │     ┌─────────┐   ┌───────────────────┐\n                │  + robust deg-4 projection (IRLS)     │     │  BLEND  │──►│ GUARDED OVERRIDE  │──► submission.csv\n                └───────────────────────────────────────┘     └─────────┘   │ exact contact     │\n                ┌───────────────────────────────────────┐          ▲        │ lookup, applied   │\n train + test ─►│ PIPELINE B · \"fleongg\"                │          │ 0.45   │ ONLY if verified  │\n                │  likelihood-weighted multi-scale PF   │─► sub_B ─┘        └───────────────────┘\n                │  + offset-well spatial priors (KNN)   │\n                │  + GBM stack (GroupKFold by well)     │\n                └───────────────────────────────────────┘\n```\n\n## Why each piece is there\n\n| Component | What it does | Why it helps |\n|---|---|---|\n| Particle filter (×128 seeds, 4 scales) | sequential Monte-Carlo tracking of TVT against the typewell GR log | physics-aware sequential signal that flat tabular models miss |\n| Beam search | global GR-alignment path candidates | catches branches a greedy tracker loses |\n| Spatial priors (plane-KNN / dense ANCC) | \"neighbouring wells share the same geology\" | stabilizes wells with weak/ambiguous GR |\n| GBM stack + Ridge meta | fuses trackers, priors, geometry, GR stats | every estimator is wrong differently; the model learns *when to trust which* |\n| Two-pipeline blend (0.55 / 0.45) | averages two independent implementations | decorrelated errors — the single biggest cheap win on this LB |\n| Robust projection (deg-4 IR",
  "code_hits": [
    [
      4,
      "from lightgbm import LGBMRegressor, log_evaluation, early_stopping | from sklearn.metrics import root_mean_squared_error | from sklearn.model_selection import GroupKFold | from sklearn.linear_model import Ridge | from catboost import CatBoostRegressor | from scipy.spatial import cKDTree | from scipy.signal import savgol_filter | from joblib import Parallel, delayed | from koolbox import Trainer | from pathlib import Path | from numba import njit | import matplotlib.pyplot as plt | import multiprocessing | import seaborn "
    ],
    [
      7,
      "SELECTOR_N_EVAL_THRESHOLD = 4840.0 | SELECTOR_Z_SPAN_THRESHOLDS = (136.73000000000016, 185.5133333333342) |  | SELECTOR_BIN_VARIANTS = { |     0: 'pf_scale_5_hold_0.2', |     1: 'pf_scale_3_hold_0.15', |     2: 'pf_scale_12_beam_0.2_hold_0.15', |     3: 'pf_scale_5_hold_0.15', |     4: 'pf_scale_5_beam_0.05_hold_0.05', |     5: 'pf_scale_12_beam_0.2_hold_0.05', | } |  | SELECTOR_GLOBAL_VARIANT = 'pf_scale_8_hold_0.2' | SELECTOR_SCALES = (3.0, 5.0, 8.0, 12.0) |  | FORMATION_COLS = ['ANCC', 'ASTNU', 'ASTNL', 'EGFDU', 'EGFDL', "
    ],
    [
      16,
      "for i, params in enumerate(lgb_params):    |     save_path = f\"models/lightgbm-{i+1}\" |      |     if (CFG.artifacts_path / save_path).exists(): |         print(f\"Loading lightgbm-{i+1} from disk...\") |          |         trainer_paths = (CFG.artifacts_path / save_path).glob('*.pkl') |         trainer = joblib.load(list(trainer_paths)[0]) |          |         print(f\"Loaded lightgbm-{i+1} with overall RMSE: {trainer.overall_score:.4f}\\n\") |     else: |       |         trainer = Trainer( |             estimator=LGBMRegres"
    ],
    [
      18,
      "for i, params in enumerate(cb_params):     |     save_path = f\"models/catboost-{i+1}\" |     if (CFG.artifacts_path / save_path).exists(): |         print(f\"Loading catboost-{i+1} from disk...\") |          |         trainer_paths = (CFG.artifacts_path / save_path).glob('*.pkl') |         trainer = joblib.load(list(trainer_paths)[0]) |          |         print(f\"Loaded catboost-{i+1} with overall RMSE: {trainer.overall_score:.4f}\\n\") |     else: |         trainer = Trainer( |             estimator=CatBoostRegressor(**p"
    ],
    [
      27,
      "sample_sub = pd.read_csv(CFG.dataset_path / \"sample_submission.csv\") | sub_1 = (sample_sub[['id']].merge( |     test_df2[['id', 'pred']].rename(columns={'pred':'tvt'}), |     on='id',  |     how='left' | )) |  | sub_1['tvt']=sub_1['tvt'].fillna(float(train_df['last_known_tvt'].mean()+train_df['target'].mean())) | sub_1"
    ],
    [
      29,
      "sample = pd.read_csv(CFG.dataset_path / 'sample_submission.csv') | sample['well']    = sample['id'].str[:8] | sample['row_idx'] = sample['id'].str[9:].astype(int) |  | train_hw_files = sorted(glob.glob(str(CFG.dataset_path / 'train' / '*__horizontal_well.csv'))) | train_wells = [os.path.basename(f).split('__')[0] for f in train_hw_files] |  | test_hw_files = sorted(glob.glob(str(CFG.dataset_path / 'test' / '*__horizontal_well.csv'))) | test_wells = [os.path.basename(f).split('__')[0] for f in test_hw_files] |  | ro"
    ],
    [
      32,
      "sub = ( |     sub_1.merge(sub_2, on='id', suffixes=('_1', '_2')) |        .assign(tvt=lambda x: 0.3 * x['tvt_1'] + 0.7 * x['tvt_2']) |        [['id', 'tvt']] | ) | sub.to_csv(\"submission.csv\", index=False) | sub"
    ],
    [
      33,
      "# === robust low-order PROJECTION post-processing (patched degree=4, blend=0.75) (CV-validated: raw PF -0.54, deployed components -0.33) === | # Runs AFTER the 0.3*ridge+0.7*selector blend writes submission.csv; OVERWRITES it with the projected | # version. Per-well robust deg-5 fit of dU = tvt + Z - anchor vs normalized MD -> denoise jitter + | # down-weight wrong-branch outliers. Deterministic; defensive per-well fallback to raw. | import numpy as _np, pandas as _pd | def _robfit(s, y, deg=5): |     if le"
    ]
  ]
}