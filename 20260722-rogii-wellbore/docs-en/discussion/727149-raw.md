Topic #727149: Is the sub-6 regime end-to-end learned, or engineered alignment? (and is it field-grouped-CV-safe?)
  Author: Murat A. Genc
  Posted: 2026-07-17 23:14:04.853000
  Votes: 1  Comments: 1

Sharing an honest baseline first: my within-field, group-by-well pipeline (structural datum + particle-filter GR tracking) sits at ~9.9 pooled RMSE on full-773 OOF, squarely in the cluster Georgy and others describe (const 9.04 / line  6.70 oracles). I've independently reproduced that the per-well drift slope is not learnable from legal features (field-grouped OOF Rﾂｲ &lt; 0, shuffle-controlled). So my questions are aimed at the gap between this ~9窶・0 cluster and the  sub-6 results:




For the ~5.7-pooled per-well models (group-by-well CV): does the model see the type-well GR-vs-TVT curve as an input channel and learn the match implicitly, or is there an explicit alignment/warp step (DTW with slope limits  particle filter / HMM-Viterbi) that produces the coordinate frame ,  i.e. a candidate TVT(MD) path ,  that the model then refines? In short: is geosteering learned end-to-end, or engineered as preprocessing?


On the shared bimodal blind spot (several people report independent models converging on the same wrong mode for long cycle-skip stretches, ~ﾂｱ90 ft): does anything break that beyond sequence smoothness? Specifically, do the  train-only formation-top columns (ANCC/ASTNU/ASTNL/EGFDU/EGFDL/BUDA) give an absolute-layer anchor that resolves the ambiguity, or is that error just accepted on the outlier wells?


On CV realism (the one that decides everything for me): ~5.3窶・.8 pooled is reported with random group-by-well, while within-field hold-out for the same method class tops out ~10. Is the difference field-grouping ,  does random  group-by-well leak within-field structure? And do the hidden test wells sit inside the train fields (making within-field interpolation legal) or in held-out fields (making it a leak)?

Comments:
笏懌楳 Georgy Mamarin (2026-07-22 11:33:57.707000) [+0]
笏・ I can answer parts of 1 and 3 with measurements, and 2 with a pointer 窶・mostly from the notebook you referenced, so the numbers are traceable.
笏・ 
笏・ 
笏・ 
笏・ 
笏・ Here is the boundary I measured. With the calibrat...
